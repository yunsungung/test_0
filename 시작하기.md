# 시작하기

**개발을 해본 적 없어도 괜찮습니다.** 이 문서만 위에서부터 따라 하면 됩니다.

명령어는 전부 복사해서 붙여넣으면 되고, 각 단계마다 **무엇이 보여야 정상인지** 적어 뒀습니다.
화면에 그게 안 보이면 그 자리에서 멈추고 확인하세요. 그냥 넘어가면 나중에 더 헤맵니다.

---

## 전체 흐름

```
[준비]  프로그램 설치 → 내 저장소 만들기 → 내 PC로 내려받기
              ↓  (처음 한 번만)
[개발]  Claude 에게 시키기 → 내 PC 브라우저에서 확인 → 검사
              ↓  (계속 반복)
[배포]  GitHub 에 올리기 → 자동 배포 → 진짜 주소에서 확인
```

준비는 처음 한 번만 하면 됩니다. 그다음부터는 개발과 배포만 반복합니다.

---

# 1부. 준비 (처음 한 번만)

## 1-1. 프로그램 3개 설치

| 프로그램 | 왜 필요한가 | 받는 곳 |
| --- | --- | --- |
| **Node.js 22** | 이 프로그램이 웹사이트를 돌립니다 | <https://nodejs.org> 에서 LTS |
| **Git** | 내 코드를 GitHub 에 올릴 때 씁니다 | <https://git-scm.com> |
| **Claude Code** | 코드를 대신 써 주는 도구입니다 | <https://claude.com/claude-code> |

설치가 끝나면 터미널(Windows 는 `PowerShell`, Mac 은 `터미널`)을 열고 확인합니다.

```bash
node --version
git --version
```

**정상이면:** `v22.x.x` 와 `git version 2.x.x` 같은 글자가 나옵니다.

**안 나오면:** 설치가 안 됐거나, 터미널을 껐다 켜야 합니다. 터미널을 완전히 닫고 다시 열어 보세요.

---

## 1-2. 내 저장소 만들기

이 템플릿을 복사해서 **내 것**을 만듭니다.

1. <https://github.com/yunsclaude/template-one> 접속
2. 초록색 **`Use this template`** 버튼 → **`Create a new repository`**
3. 입력하고 만들기

| 칸 | 넣을 값 |
| --- | --- |
| Owner | `yunsclaude` |
| Repository name | 내 프로젝트 이름 (영문 소문자, 숫자, 하이픈만. 예: `hr-notice`) |
| Public / Private | **Public** |

> **Public 을 고르세요.** 회사 조직에 등록된 배포 설정이 Private 저장소에는 전달되지 않아
> 배포가 실패합니다. 사내 도구라 코드가 공개돼도 문제없지만, 비밀번호나 API 키는
> 절대 코드에 적지 마세요. (아래 [4-2](#4-2-api-키가-필요하다면) 참고)

**정상이면:** 내 저장소가 만들어지고, 파일 목록에 `app`, `components`, `lib` 같은 폴더가 보입니다.

**`Use this template` 버튼이 없다면:** 인프라 담당에게 알려 주세요. 템플릿 설정이 꺼져 있는 겁니다.

---

## 1-3. 내 PC 로 내려받기

터미널을 열고, 프로젝트를 둘 폴더로 이동한 뒤 실행합니다.
(`<내저장소이름>` 은 방금 만든 이름으로 바꾸세요.)

```bash
git clone https://github.com/yunsclaude/<내저장소이름>.git
cd <내저장소이름>
npm ci
```

`npm ci` 는 필요한 부품을 내려받는 과정입니다. **1~3분 걸립니다.** 기다리세요.

**정상이면:** `added 300 packages` 같은 글자가 나오고 오류 없이 끝납니다.

---

## 1-4. 내 프로젝트 정보 적기

`project.json` 파일을 엽니다. 메모장으로 열어도 되고, Claude 에게 시켜도 됩니다.

```json
{
  "id": "hr-notice",
  "name": "인사 공지 게시판",
  "description": "부서별 공지를 모아 보는 사내 도구입니다.",
  "owner": "김철수",
  "department": "인사팀",
  "domain": "",
  "port": 3000,
  "database": false
}
```

| 항목 | 무엇을 적나 |
| --- | --- |
| `id` | **접속 주소가 됩니다.** 영문 소문자, 숫자, 하이픈만. 저장소 이름과 같게 하면 편합니다 |
| `name` | 화면에 보일 이름. 한글 가능 |
| `description` | 이 도구가 무엇을 하는지 한 줄 |
| `owner` | 내 이름 |
| `department` | 내 부서 |
| `domain` | **비워 두세요.** 배포할 때 자동으로 채워집니다 |
| `port` | **3000 그대로 두세요.** 바꾸면 배포가 안 됩니다 |
| `database` | 데이터를 저장해야 하면 `true`, 아니면 `false` ([4-1](#4-1-데이터를-저장하고-싶다면) 참고) |

> **`id` 는 다른 사람과 겹치면 안 됩니다.** 겹치면 배포할 때 거부되고 이유를 알려 줍니다.
> `n8n`, `dev`, `web` 처럼 회사가 이미 쓰는 이름도 거부됩니다.

---

## 1-5. 배포 설정 확인

저장소 → **Settings** → **Secrets and variables** → **Actions**

여기에 아래 4개가 보여야 합니다. **조직에 이미 등록돼 있으면 아무것도 안 해도 됩니다.**

| 탭 | 이름 |
| --- | --- |
| Secrets | `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY` |
| Variables | `BASE_DOMAIN` |

**안 보인다면** 인프라 담당에게 "조직 Secret 이 내 저장소에 전달되는지 확인해 주세요" 라고 요청하세요.
개인키를 직접 받아서 붙여넣는 것보다 그게 안전합니다.

준비 끝입니다. 이제부터가 진짜 개발입니다.

---

# 2부. 개발하기 (계속 반복)

## 2-1. Claude 에게 시키기

프로젝트 폴더에서 터미널을 열고 실행합니다.

```bash
claude
```

그다음 **한국어로 그냥 말하면 됩니다.**

```
부서별 공지를 목록으로 보여 주는 화면을 만들어 줘.
공지는 제목, 작성자, 날짜가 있어.
```

잘 시키는 요령

- **한 번에 하나씩.** "게시판 만들고 로그인도 넣고 알림도 보내줘" 보다 하나씩 나눠서.
- **화면을 설명하세요.** "위에 제목이 있고 아래에 목록이 나오게" 처럼 눈에 보이는 대로.
- **막히면 물어보세요.** "이 오류가 무슨 뜻이야?" 라고 그대로 붙여넣으면 됩니다.
- **끝나면 확인 요청.** "`npm run check` 돌려서 통과하는지 확인해 줘"

> Claude 는 `CLAUDE.md` 에 적힌 회사 규칙을 지키도록 되어 있습니다.
> 그래서 "이건 규칙상 안 됩니다" 라고 답할 때가 있는데, 정상입니다.

---

## 2-2. 내 PC 에서 눈으로 확인하기

**이게 가장 중요한 단계입니다.** 올리기 전에 반드시 여기서 확인하세요.

```bash
npm run dev
```

**정상이면:** 이런 글자가 나옵니다.

```
▲ Next.js 16.3.0
- Local:  http://localhost:3000
✓ Ready in 1.2s
```

이제 브라우저를 열고 주소창에 입력합니다.

```
http://localhost:3000
```

**내가 만든 화면이 보입니다.** 여기서 클릭도 해보고 입력도 해보세요.

### 고치면서 보기

`npm run dev` 를 켜 둔 채로 Claude 에게 계속 시키세요.
코드가 바뀌면 **브라우저가 저절로 새로고침됩니다.** 껐다 켤 필요 없습니다.

### 끝낼 때

터미널에서 **`Ctrl + C`** 를 누릅니다. (Mac 도 `Control + C` 입니다.)

### 잘 안 될 때

| 증상 | 해결 |
| --- | --- |
| `Port 3000 is in use` | 이미 켜져 있습니다. 다른 터미널에서 `Ctrl + C` 로 끄세요 |
| 브라우저가 "연결할 수 없음" | `npm run dev` 가 켜져 있는지, `Ready` 가 나왔는지 확인 |
| 화면이 안 바뀜 | 브라우저에서 `Ctrl + Shift + R` (Mac 은 `Cmd + Shift + R`) |
| 빨간 오류 화면 | 그 내용을 그대로 복사해 Claude 에게 붙여넣으세요 |

---

## 2-3. 검사하기

올리기 전에 반드시 실행합니다.

```bash
npm run check
```

5가지를 순서대로 검사하고, **하나라도 실패하면 거기서 멈춥니다.**

| 순서 | 무엇을 보나 |
| --- | --- |
| 1 | `project.json` 이 규칙에 맞는지 |
| 2 | **API 키가 코드에 적혀 있지 않은지** |
| 3 | 코드 스타일 |
| 4 | 타입 오류 |
| 5 | 실제로 빌드가 되는지 |

**정상이면:** 마지막에 오류 없이 끝나고 `Route (app)` 이라는 표가 나옵니다.

**실패하면:** 오류 내용을 **그대로 복사해서** Claude 에게 주세요.

```
npm run check 했더니 이렇게 나왔어. 고쳐줘.
(여기에 붙여넣기)
```

> `[주의]` 로 시작하는 노란 글자는 실패가 아닙니다. "이건 확인해 보세요" 라는 알림입니다.
> 예를 들어 `id` 를 템플릿 예시값 그대로 두면 알려 줍니다.

---

## 2-4. GitHub 에 올리기

### 가장 쉬운 방법: Claude 에게 시키기

```
지금까지 작업한 거 커밋하고 푸시해줘
```

Claude 가 알아서 처리합니다. 무엇을 올릴지 먼저 보여 주고 물어봅니다.

### 직접 하려면

```bash
git add .
git commit -m "공지 목록 화면 추가"
git push
```

`-m` 뒤에는 **무엇을 했는지** 한글로 적으면 됩니다.

**정상이면:** `main -> main` 같은 글자가 나옵니다.

### 커밋이 거부됐다면

```
[실패] 소스에 비밀값으로 보이는 내용이 있습니다.
  app/page.tsx:12
```

**API 키를 코드에 적어서 막힌 겁니다.** 잘 막힌 겁니다. 아래 [4-2](#4-2-api-키가-필요하다면) 를 보세요.

---

# 3부. 배포하고 확인하기

## 3-1. 자동 배포 지켜보기

`main` 에 올라가면 **자동으로 배포가 시작됩니다.** 따로 할 일이 없습니다.

저장소 → 위쪽 **`Actions`** 탭

| 표시 | 뜻 |
| --- | --- |
| 🟡 노란 점 | 배포 중입니다. 3~5분 걸립니다 |
| ✅ 초록 체크 | **성공.** 이제 접속됩니다 |
| ❌ 빨간 X | 실패. 아래 참고 |

### 빨간 X 가 떴다면

1. 빨간 줄을 클릭
2. `Error:` 로 시작하는 빨간 글자를 찾습니다
3. 그 문장을 복사해 Claude 에게 주세요

자주 나오는 것들

| 메시지 | 뜻 |
| --- | --- |
| `BASE_DOMAIN 이 비어 있습니다` | 배포 설정 미등록. [1-5](#1-5-배포-설정-확인) 참고 |
| `프로젝트 ID ... 를 이미 다른 프로젝트가 쓰고 있습니다` | `id` 가 겹칩니다. `project.json` 에서 바꾸세요 |
| `... 이(가) 이 배포 서버가 아닌 다른 곳을 가리킵니다` | 회사가 이미 쓰는 이름입니다. `id` 를 바꾸세요 |
| `소스에 비밀값으로 보이는 내용이 있습니다` | 코드에 API 키가 있습니다 |

**실패해도 이미 돌아가던 화면은 그대로 살아 있습니다.** 안심하고 고치세요.

---

## 3-2. 진짜 주소에서 확인하기

초록 체크가 뜨면 브라우저에서 엽니다.

```
https://<project.json 의 id>.<회사 도메인>
```

`id` 가 `hr-notice` 라면 `https://hr-notice.회사도메인` 입니다.
회사 도메인은 인프라 담당이 이미 등록해 둔 값이라 신경 쓰지 않아도 됩니다.

**정상이면:** 자물쇠 아이콘(🔒)이 보이고 내 화면이 뜹니다.

이 주소를 **동료에게 그냥 보내면 됩니다.** 설치도 설정도 필요 없습니다.

### 확인용 주소

```
https://<내주소>/api/health
```

이렇게 나오면 서버가 정상입니다.

```json
{"status":"ok","projectId":"hr-notice", ...}
```

### 화면이 안 바뀐 것 같다면

1. Actions 가 **초록 체크**인지 확인 (노란 점이면 아직 배포 중)
2. 브라우저 강력 새로고침: `Ctrl + Shift + R` (Mac 은 `Cmd + Shift + R`)

---

# 4부. 자주 필요한 것들

## 4-1. 데이터를 저장하고 싶다면

게시글, 신청 내역처럼 **저장했다가 다시 봐야 하는 것**이 있으면 데이터베이스를 씁니다.

`project.json` 에서 한 글자만 바꾸면 끝입니다.

```json
"database": true
```

이러면 배포할 때 **내 전용 데이터베이스가 자동으로 만들어집니다.**
비밀번호를 만들거나 어딘가에 등록할 필요가 없습니다. 다른 사람은 내 데이터를 볼 수 없습니다.

내 PC 에서 개발할 때는 Docker 로 데이터베이스를 함께 띄웁니다.

```bash
docker compose up -d db   # 데이터베이스만 켜기
npm run dev               # 그다음 개발 서버
```

그리고 Claude 에게 시키면 됩니다.

```
공지 목록을 데이터베이스에 저장하도록 바꿔줘
```

> Docker 가 없다면 <https://docker.com> 에서 Docker Desktop 을 설치하세요.

---

## 4-2. API 키가 필요하다면

OpenAI 같은 외부 서비스를 쓰려면 키가 필요합니다.

### 절대 하면 안 되는 것

```ts
const apiKey = "sk-진짜키값";   // ❌ 이러면 막힙니다
```

**코드에 적으면 커밋도 배포도 거부됩니다.** GitHub 에 한 번 올라가면 전 세계가 볼 수 있고,
지워도 기록에 남기 때문입니다.

### 올바른 방법

**① 코드에서는 이렇게 씁니다** (Claude 에게 시키면 알아서 이렇게 해줍니다)

```ts
const apiKey = process.env.OPENAI_API_KEY;
```

**② 내 PC 용** — 프로젝트 폴더에 `.env.local` 이라는 파일을 만들고 적습니다.

```
OPENAI_API_KEY=sk-진짜키값
```

이 파일은 GitHub 에 올라가지 않습니다.

**③ 배포 서버용** — 저장소 → Settings → Secrets and variables → Actions → Secrets 탭
→ `New repository secret`

| 칸 | 값 |
| --- | --- |
| Name | `APP_ENV` |
| Secret | `.env.local` 내용을 **그대로 복사해 붙여넣기** |

```
OPENAI_API_KEY=sk-진짜키값
SLACK_BOT_TOKEN=xoxb-진짜토큰
```

키가 여러 개면 **줄을 나눠서 다 넣으면 됩니다.** Secret 을 여러 개 만들지 마세요.

> 키를 바꾸거나 추가했으면 `APP_ENV` 를 고친 뒤 **다시 배포해야** 반영됩니다.

### 실수로 키를 올렸다면

**파일에서 지우는 것으로는 부족합니다.** 기록에 남아 있습니다.

1. 키 발급처(OpenAI 등)에서 그 키를 **폐기(revoke)** 합니다
2. 새 키를 발급받습니다
3. 위 방법대로 다시 넣습니다

부끄러워할 일이 아닙니다. 바로 폐기하는 것이 가장 중요합니다.

---

## 4-3. 명령어 모음

| 하고 싶은 것 | 명령 |
| --- | --- |
| 부품 설치 (처음 한 번) | `npm ci` |
| 개발하면서 화면 보기 | `npm run dev` → <http://localhost:3000> |
| 데이터베이스도 함께 | `docker compose up -d db` |
| 올리기 전 검사 | `npm run check` |
| Claude 실행 | `claude` |
| 개발 서버 끄기 | `Ctrl + C` |

---

## 4-4. 건드리면 안 되는 파일

이 파일들은 **배포가 의존**합니다. 실수로 지우거나 고치면 배포가 깨집니다.

```
Dockerfile          compose.yaml         compose.prod.yaml
next.config.ts      .github/ 폴더        scripts/ 폴더
.claude/ 폴더       .githooks/ 폴더
```

Claude 도 이 파일들은 함부로 고치지 않도록 되어 있습니다.
꼭 고쳐야 하는 상황이면 인프라 담당에게 먼저 물어보세요.

---

## 4-5. 막혔을 때

**대부분은 Claude 에게 물어보면 해결됩니다.** 오류 메시지를 그대로 복사해서 붙여넣으세요.

```
이런 오류가 났어. 무슨 뜻이고 어떻게 고쳐?
(여기에 붙여넣기)
```

Claude 로도 안 되면 인프라 담당에게 아래 3가지를 알려 주세요.

1. 저장소 주소
2. 무엇을 하려다 막혔는지
3. 오류 메시지 전체 (일부만 말고 전부)

---

## 더 자세한 내용

- [`README.md`](README.md) — 폴더 구조, 배포 구조 등 자세한 설명
- [`CLAUDE.md`](CLAUDE.md) — Claude 가 지키는 규칙 (읽어 두면 왜 그렇게 답하는지 이해됩니다)
